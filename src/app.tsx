// utils
import CSModel from './CSModel';
import { renderHeader, renderFooter } from './helper';
// css
import './app.scss';

const model = new CSModel();

// header
renderHeader(model);
// footer
// renderFooter(model);